export enum RouteName {
  SigningRequest = 'sign_wallet',
  SigningTransaction = 'sign_transaction',
  EncryptionRequest = 'encryption_request',
  Main = 'main',
}

export enum MainRouteName {
  CreateThread = 'create_thread',
  Thread = 'thread',
}

export enum ThreadRouteName {
  Messages = 'messages',
  Settings = 'settings',
}
